{
  "name": "unlimited-ocr-frontend",
  "private": true,
  "type": "module",
  "description": "Unlimited-OCR 정적 프론트엔드 — 빌드 스텝/런타임 의존성 없음. 이 파일은 Node 테스트 러너가 app.js를 ES 모듈로 임포트하기 위해서만 존재한다 (브라우저/서버 동작에는 영향 없음). playwright는 test:e2e 전용 devDependency.",
  "scripts": {
    "test": "node --test tests/",
    "test:e2e": "node tests/e2e/ui.e2e.mjs"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
